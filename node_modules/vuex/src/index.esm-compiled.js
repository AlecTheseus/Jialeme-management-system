import { Store, install } from './store';
import { mapState, mapMutations, mapGetters, mapActions } from './helpers';

export default {
  Store: Store,
  install: install,
  version: '__VERSION__',
  mapState: mapState,
  mapMutations: mapMutations,
  mapGetters: mapGetters,
  mapActions: mapActions
};

export { Store, mapState, mapMutations, mapGetters, mapActions };

//# sourceMappingURL=index.esm-compiled.js.map
var ometa = require('./ometa-node');

var createParser = function createParser(grammar, parserCallback) {
  var parser;
  try {
    parser = ometa(grammar);
    parserCallback(null, {
      parse: function parse(code, rule, callback) {
        callback(null, parser(code, rule));
      }
    });
  } catch (err) {
    parserCallback({
      inner: err
    });
  }
};

module.exports.createParser = createParser;

//# sourceMappingURL=index-compiled.js.map